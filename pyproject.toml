[project]
name = "website"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.3",
    "jupyter>=1.1.1",
    "matplotlib>=3.10.9",
    "numpy>=2.4.6",
    "polars>=1.41.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
